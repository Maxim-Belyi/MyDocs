---
sidebar_position: 7
---

#### **7. Что такое циклы?**

Цикл в программировании — это управляющая конструкция, которая позволяет многократно выполнять один и тот же блок кода до тех пор, пока не будет выполнено заданное условие или пока не будет достигнуто определенное количество повторений, что автоматизирует повторяющиеся задачи и делает код компактнее. Это ключевой инструмент для обработки коллекций (списков, строк), моделирования процессов, игр, и обработки данных.

## Основные типы циклов   
* for (Цикл со счетчиком): Используется, когда известно точное количество повторений. Он инициализирует счетчик, проверяет условие и обновляет счетчик на каждой итерации. Пример: for i in range(5): - повторить 5 раз.

* while (Цикл с предусловием): Выполняется, пока условие истинно. Проверка условия происходит перед выполнением тела цикла. Если условие ложно с самого начала, тело цикла не выполнится ни разу. Пример: while (x < 10): - повторять, пока \(x\) меньше 10.

* do-while (Цикл с постусловием): Тело цикла выполняется хотя бы один раз, а затем проверяется условие 
